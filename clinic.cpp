#include <iostream>
#include <vector>
#include <queue>
#include <unordered_set>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long N, K;
    cin >> N >> K;
    int q;
    vector<long> v;

    priority_queue<string> pq;

    while(N--){
        cin >> q;
        switch (q){
            case 1:

                break;
            case 2:
                break;
            case 3:
                break;
        }
    }

}
